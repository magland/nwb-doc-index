You will receive a document from the user. Respond only with a concise summary (1–2 sentences) that captures the document’s main topic and key contents. This summary will be used directly as a description in a searchable index to help an AI agent determine which documents to retrieve for a given query. Include enough detail to convey what the document is about and what information it contains, but keep it brief to minimize token usage. Do not include any additional commentary, formatting, or explanation—only the summary.